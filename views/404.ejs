<h1>
    <%= title %>
</h1>

<% if (typeof errors !=="undefined" && errors.length> 0) { %>
    <ul style="color:red;">
        <% errors.forEach(err=> { %>
            <li>
                <%= err.msg %>
            </li>
            <% }) %>
    </ul>
    <% } %>

        <a href="/">⬅ Go back to Home</a>